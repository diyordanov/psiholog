# Раздел 9. Заключение

## 9.1 Постигнати резултати

В резултат на настоящата дипломна работа е реализирана пълноценна платформа за цифрово подписване на PDF документи, съчетаваща класическа и постквантова криптография — ECDSA P-256 и ML-DSA-65 (NIST FIPS 204) — в единен хибриден подпис, съвместим с индустриалния стандарт PAdES и потвърдено разпознаваем от Adobe Acrobat Reader (Раздел 7.1). Частните ключове никога не напускат браузъра на потребителя: те са защитени чрез WebAuthn PRF extension, при което биометрията служи единствено като локален derivation фактор, а не като предаван по мрежата секрет. Платформата поддържа multi-signer workflow с паралелно подписване от няколко участника, оптимистично управление на конкурентен достъп при едновременни промени и собствен mini-CA за издаване на leaf сертификати без зависимост от платена трета страна. Коректността на реализацията е потвърдена от 204 автоматизирани unit теста (всички успешни), допълнени от ръчни end-to-end и Row Level Security regression проверки. Накрая, верификацията на подписан документ е достъпна публично, без регистрация, и се изпълнява изцяло в браузъра — самият файл никога не напуска устройството на проверяващия.

## 9.2 Ограничения

Реализацията има и известни ограничения, които не се крият:

- Пълно ръчно тестване е проведено само в Chrome/Edge (Chromium); Firefox и Safari се очаква да работят по спецификация, но не са изчерпателно верифицирани (Раздел 7.3).
- Не е реализирана интеграция с Time Stamp Authority (TSA) — постигнатото ниво на съответствие е PAdES-B-Basic, не PAdES-B-T.
- Известията към получатели в multi-signer flow-а използват стандартния Supabase email шаблон, не брандирано съобщение.
- UI интерфейсът за multi-signer поддържа собственик + до двама получатели; backend моделът (таблична схема с `version` брояч) поддържа произволен брой участници, но не е изложен през интерфейса.
- Bundle размерът на приложението (~901 KB gzip, Раздел 7.5) е над препоръчителния праг от 500 KB за бърз mobile load.
- Тестване със screen reader (VoiceOver, NVDA) не е формално документирано — наличният Accessibility одит (Раздел 7.4) покрива статичен DOM/ARIA анализ, не ръчно преминаване с асистивна технология.
- Собственият Root CA е академичен по своята същност — не е част от Adobe Approved Trust List (AATL) или еквивалентен публичен trust list.
- Не са реализирани механизми за отмяна или изтичане на срока на валидност на подпис (CRL/OCSP revocation checking, signature expiry).

## 9.3 Бъдеща работа

**Криптография.** Интеграция с Time Stamp Authority би повдигнала съответствието от PAdES-B-Basic до PAdES-B-T, добавяйки доказуемо време на подписване, независимо от системния часовник на подписващия. Следваща стъпка след това е PAdES-LTV профилът, при който DSS (Document Security Store) и VRI (Validation Related Information) речници вграждат в самия документ всичко нужно за дългосрочна валидация — сертификатна верига и статус на отмяна — дори след изтичане на самите сертификати. Липсващият revocation checking (CRL/OCSP) е естествено продължение на същата линия. От страна на recovery flow-а, поддръжка на множество регистрирани passkey устройства би премахнала единичната точка на отказ при загубено устройство.

**Multi-signer workflow.** Освен настоящия паралелен модел, последователно подписване (sequential signing, при което получател N+1 вижда документа едва след подписване от получател N) е логично разширение за workflow-и, изискващи строг ред. Автоматични напомняния за просрочени покани (чрез `pg_cron` scheduled job) и брандирана email комуникация (напр. през Resend с собствен domain) биха доближили продукта до production-ready DocuSign алтернатива. Push нотификации за pending покани биха намалили латентността между покана и реакция.

**Инфраструктура.** Намаляването на bundle размера чрез динамичен import (`dynamic import()`) на PDF стека (`pdf-lib`, `pdfjs-dist`, `@pdf-lib/fontkit`) — заедно с по-агресивна CDN caching стратегия — адресира директно ограничението от Раздел 7.5. Преминаването от академичен към публично признат Root CA (AATL или EUTL trust list) е предпоставка платформата да се възприема като production-ready извън демонстрационен контекст.

**UX.** Довършване на изчерпателно тестване във Firefox и Safari, формален screen reader тест (VoiceOver, NVDA) вместо само статичен ARIA одит, и целенасочена мобилна оптимизация (интерфейсът е responsive, но не е стрес-тестван на реални устройства с ниска производителност) остават отворени точки. Интерфейсът в момента е единствено на български; въвеждане на I18n framework би отворило платформата към по-широка аудитория без архитектурна промяна.

**Академични разширения.** Сравнителен анализ на ML-DSA-65 спрямо алтернативни постквантови схеми (Falcon, SPHINCS+) по критерии размер на подпис, скорост и bundle въздействие би задълбочил обосновката на избора, направен в Раздел 3/4. Реални измервания на мобилни устройства (а не само desktop, както в Раздел 7.5) биха потвърдили или коригирали текущите оценки за производителност. Независим security audit от трета страна е естествената следваща стъпка преди каквото и да е производствено внедряване.

SignShield демонстрира, че хибридна post-quantum PDF signing е практически реализуема днес — не само теоретично осъществима, а работещ продукт, съвместим с индустриален стандарт (Adobe Reader), с измерена производителност в порядъка на десетки милисекунди дори за постквантовите операции, и с архитектура, която не изисква компромис между сигурност и достъпност на частния ключ за крайния потребител.
